NODE_ENV=development
PORT=3000
HOST=localhost
LOG_LEVEL=info

# API Authentication
# Comma-separated list of valid API keys for x-api-key header
# In production, these will be provided by OpenShift as secrets
API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/api-service-poc

# MongoDB Connection Pool Settings
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=2

# Database Authentication (Development/Non-Production)
# Uses username/password when NODE_ENV != production
DB_USERNAME=
DB_PASSWORD=

# Database Authentication (Production Only)
# Uses certificates when NODE_ENV=production
# DB_CERT_KEY_PATH=/path/to/mongodb-cert-key.pem
# DB_CA_PATH=/path/to/ca-cert.pem
